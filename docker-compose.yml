version: '3.8'

services:
  gen-studio:
    build: .
    container_name: gen_studio_app
    ports:
      - "5000:5000"
    environment:
      
      - FLASK_ENV=development
    volumes:
      
      - .:/app
    restart: unless-stopped